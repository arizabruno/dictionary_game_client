class Guess {
    constructor(sid, definition, user, word) {
        this.sid = sid;
        this.definition = definition;
        this.user = user;
        this.word = word;
    }
}
  
export default Guess;